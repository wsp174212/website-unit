# Agent 工作约定

> 本文件记录本次协作中关于临时文件、进程和 Git 卫生的固定期望。
> 可复制到其他项目，作为给 agent 的执行约束。

## 1. 先说明，再执行

凡是会启动进程、创建临时目录、可能生成大量文件或改变 Git 状态的操作，
agent 必须先用简短计划说明：

- 要做什么
- 为什么做
- 会产生什么副作用
- 如何清理

可能产生大量文件或明显副作用时，先获得用户确认。

## 2. 临时产物不进仓库

- 测试数据、浏览器 profile、缓存、日志一律放在系统临时目录。
- 不在仓库内使用 `--user-data-dir`、缓存目录或临时数据目录。
- 临时目录命名带项目和用途，例如 `siteunit-stats-verify`。
- 验证结束立即清理。

## 3. 进程必须闭环

启动本地服务或浏览器前记录端口和 PID；验证完成后立即停止。
如果用户中断任务，要在恢复时先检查并清理残留进程。

## 4. Git 保持干净

- 只 add 明确需要的文件。
- 提交前查看 `git show --stat`，确认没有运行时产物。
- 不用会输出大量 untracked 文件的方式检查状态；优先使用定向路径。
- 不提交 `data/`、缓存、浏览器 profile、日志、虚拟环境等产物。

## 5. 控制上下文成本

磁盘上存在大量文件本身不等于消耗 token；
真正消耗 token 的是工具命令输出和文件内容。
因此不要递归列出或读取大型临时目录。